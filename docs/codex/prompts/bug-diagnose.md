Use the repo-local skill `debug-failing-workflow`.

Read `AGENTS.md` first.
Then read the relevant docs under `docs/codex/`.

Bug:
[PASTE BUG HERE]

Requirements:
- restate the failure clearly
- identify reproduction steps
- identify the failing subsystem
- state the root-cause hypothesis before editing
- list the likely files/classes/functions involved
- propose the smallest safe fix
- define a regression check
- call out remaining risks or unknowns

Do not edit code yet.
Preserve grading report format unless the bug is in report formatting.
Be conservative around git write and push behavior.