Read `AGENTS.md` first.
Use the repo-local skill `debug-failing-workflow`.

Implement the approved minimal fix for:
[PASTE BUG OR APPROVED PLAN HERE]

Constraints:
- change as little code as possible
- preserve current behavior outside the bug fix
- follow the Checkstyle rules referenced by `AGENTS.md`
- do not change report format unless required by the fix
- do not introduce risky git behavior changes
- do not invent new build or test commands

After implementation:
- run the smallest relevant verification path
- report exactly what was verified
- summarize each changed file and why it changed
- list remaining risks or unverified assumptions